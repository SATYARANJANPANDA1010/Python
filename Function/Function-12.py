# Function with arguments / parameters
# Function without arguments not receive any values.
# Function with arguments/parameters recieve values at the time of calling / invoke
# A function required input to perform some operation , it is defined with arguements/parameters


def fun1(a,b):
    print("inside function")
    c=a+b
    return c

value=fun1(10,20)
print(value)
