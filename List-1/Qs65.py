# clear() method
# s.clear() --> remove all the elements from the list.

# remove all items from s
# same as --> del s[:]

list1 = list(range(1, 11))
print(list1)
list1.clear()
print(list1)
