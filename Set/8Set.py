# remove duplicate elements from the list using Set.
list1 = [10, 10, 20, 30, 50, 40, 20]
# and create a sorted list
list2 = sorted(set(list1))
print(list2)

# sorted() method is used because , It is used in collection or iterables
# It will return a sorted list
